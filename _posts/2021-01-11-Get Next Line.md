---
layout: post
title:  "Get Next Line"
date:   2021-01-11T12:00:00-00:00
author: Taewan Kim
categories: Dummy
---

# Get Next Line

#### 시작하기 전에
GNL에 대한 설명에 앞서 필요한 사전 지식에 대해 알아보도록 한다.<br/>

#### static (정적 변수)
정적 변수는 프로그램 실행 전반에 걸쳐 변수의 수명이 유지된다. 즉, 메인이 끝나기 전까지 살아있는 변수로 함수에 선언되어 있는 경우 호출 시마다 그 값을 기억하고 있게 된다. 컴파일시 메모리가 할당되므로 동적 할당과 자동 할당과는 다르다.<br/>

아래 코드는 정적 변수의 간단한 예시이다.<br/>

```c
void  addone()
{
    static int  num1 = 0;
    
    num1++;
    printf("%d\n", num1);
}

int   main(void)
{
    addone();
    addone();
    addone();
    return (0);
}
```
위와 같은 경우 출력은 아래와 같다.<br/>

```
1
2
3
```
즉, 정적 변수로 선언된 경우 이전 작업값을 기억하고 계속 살아있게 된다.<br/>

그렇다면 정적 변수는 gnl의 어느 부분에서 활용되는 것일까? gnl은 파일에서 내용을 읽어 반환하는 기능을 수행하는데 읽은 파일의 내용 중 반환되지 않는 파일의 내용이 남아있을 것이다. 이 경우 이 내용들을 기억해놓을 필요성이 있다. 이 부분에서 정적 변수가 활용되게 된다.<br/>

#### 개념
Get Next Line(GNL)의 기능은 파일을 내용을 읽어 개행 문자를 기준으로 한 줄씩 반환해주는 기능을 한다. 파일은 file descriptor 파트에서 설명했던 개념을 통해 open 함수로 열어 fd를 할당받아 접근하게 된다.<br/>

#### 기본 형태
```c
int     get_next_line(int fd, char **line)
int     get_next_line(파일 디스크립터, 반환할 문장)
```

#### GNL 반환값
파일의 내용이 남은 경우 : 1<br/>
파일의 내용이 없는 경우 : 0<br/>
오류가 발생한 경우 : -1<br/>

#### 알고리즘
당연히 구현은 사람마다 다르기 때문에 정해진 방식은 없다. 여기선 내가 구현한 GNL을 구현으로 설명이 진행될 것이고 구현에 있어 2가지 방법에 대해 제시할 것이다. 아래 구현 방식들은 fd를 동시에 여러개 사용하는 경우를 상정하였다.<br/>

먼저, GNL의 전체적인 알고리즘이다.<br/>

<p align="center">
  <img src="https://user-images.githubusercontent.com/48250370/104128785-dea88b00-53ac-11eb-88bd-56448f2565c4.png" width="500">
</p>

구현 방식에 따라 조금씩 차이가 있을 수 있다. 대략적인 흐름은 위와 같으며 파일의 내용을 읽고 개행문자가 있을 때까지 붙여주는 과정을 반복한다. 이후 개행 문자가 있으면 개행문자를 기준으로 앞뒤로 분할 앞쪽은 주소를 반환하고 뒤쪽은 저장해둔다. 이후 gnl이 다시 호출되면 저장된 문자열을 포함하여 개행문자 검사를 진행한다.<br/>

그럼 이제 2가지 방법으로 gnl에 대해 접근해보자.<br/>

#### 배열 구현
배열 구현의 경우 fd의 고유성을 이용하였다. 배열은 arr[0], arr[2], arr[10] 등의 형태로 접근할 수 있는데 fd를 할당 가능한 최대 크기만큼 배열의 크기를 미리 할당해두는 것이다. 이 경우 fd의 값에 따라 배열의 특정 위치에 쉽게 접근이 가능하다. 또한, 배열의 각 요소에 저장할 문자열의 주소값을 넣어놓는다면 아주 쉽게 해결할 수 있다.<br/>

<p align="center">
  <img src="https://user-images.githubusercontent.com/48250370/104129166-e8cb8900-53ae-11eb-9360-2892ffefaf87.png" width="500">
</p>

#### 장점
- 접근이 쉽다.
- 간결하고 이해가 쉽다.

#### 단점
- fd가 적게 쓰일수록 메모리 낭비가 심해진다.
- fd 할당의 최대 크기는 환경마다 달라 안정성이 떨어진다.

#### 연결리스트 구현
연결 리스트의 경우 새로운 fd를 사용할 경우 연결 리스트에 추가하고 다 사용한 경우 이를 제거해 주는 방식이다. 또한, 필요한 fd를 연결 리스트에서 검색하여 작업을 진행한다.<br/>

<p align="center">
  <img src="https://user-images.githubusercontent.com/48250370/104129625-106f2100-53b0-11eb-85e1-56809bcd5d1b.png" width="500">
</p>

#### 장점
- 메모리 낭비가 적다.
- 환경에 상관없이 자유롭다.

#### 단점
- 연결 리스트에 대해 이해가 부족한 경우 구현이 까다로울 수 있다.
- 배열 형태에 비해 동작 과정이 복잡하다.

#### 참고자료
https://ko.wikipedia.org/wiki/%EC%A0%95%EC%A0%81_%EB%B3%80%EC%88%98<br/>

